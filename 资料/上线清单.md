# v1.0 上线清单（B 汇总 · A 施工 · B 验收）

> 2026-09-14 汇总。把散在七份文档里的待上线项收拢成一张表：**状态列 = 截至 2026-09-14 01:00 的实装核验结果**。
> 流程：A 按本表清空"待做" → B 按 验收清单.md 全量复验 → 打包 zip → 部署（部署前 diff：线上 vs 本地必须一致，上次发的是旧构建）。

---

## 一、实装状态总表

| # | 模块 | 规格 | 状态 | 待做（A） |
|---|---|---|---|---|
| 1 | 核心循环（派遣/事件/结算/升级/KPI） | B-1 v1.1 | ✅ 已上线 | 按 B-1 全量替换占位数值；酒单临时默认 B-1 版（40/90/160/300，+40） |
| 2 | 武器模组 + 三提石抽卡 | 武器模组系统 v1.2（99 条） | ✅ 已实装已验收（05:20） | 回归测试即可 |
| 3 | 晋升 24 档 + 混编小队 | 用户拍板 v0.4 | ✅ 已上线 | — |
| 4 | 战役任务链（10 条）+ 交易市场 + 每日简报 | A 自研 | ✅ 已上线 | 文案接 TEXT（映射表二期） |
| 5 | **TEXT 全量接线（311 键）** | 文案包 v1.4 + TEXT注入包.js | 🔶 **常量已入、接线 10/300+ 处** | 按映射表一期（12 区块）+二期（A-E 节）逐条替换；注入包已备好无需拼装 |
| 6 | 梗事件（10 事件 + 头图 9 张） | 文案包 v1.1 + 数值表 §7 | 🔶 常量+头图就绪 | 触发接线（35% 槽）+ ev_* 梗文案键接入 + meme_*.png 挂弹窗 |
| 7 | 深潜双轨（WEEKLY_DIVE） | 深潜系统设计 双轨版 | ✅ 重写完成，第 5 轮复验通过（evW/checkT/modEff 修复确认） | 编年史节点接管战役解锁（见 #10） |
| 8 | **饰品系统** | 饰品系统设计（24 条） | 🔶 骨架（掉落+日历） | 佩戴 UI + tr_* 12 键 + 三提石饰品箱(25%) + 节日战役掉落接线 |
| 9 | **精英支援位** | 精英支援位系统 B-6（含匠器+团灭救援） | 🔶 常量注入 | 购买(调令)/随队/结算五效果/回溯者每周 1 次/团灭救援/RELICS 匠器——el_* 12 键接入 |
| 10 | **日期与编年史** | 日期与编年史系统 B-7 | ❌ 未实装 | CHRONICLE JSON 注入；gameDay→日期纯函数；CAMP_DAYS 废弃改节点驱动；节日档期改编年史判据；2026-09-14 终局→无尽 |
| 11 | 手机端修复 | 手机端显示修复单 | 🔶 修1/修5 ✅ 修2 未做（1 行 CSS）+ 修3/4 待验 | 修2：断点内加 `.mcard .chip{white-space:normal}`；微信提示条已接 |
| 12 | **PWA** | 用户拍板 | 🔶 备料完成 | **B 已预制 `manifest.webmanifest` + `sw.js`（root 下）**：A 只需在 游戏.html `<head>` 加 4 行 link/meta + 3 行 SW 注册（代码在 sw.js 头注释里）；file:// 与微信内自动跳过 |
| 13 | 数值对齐终检 | 验收清单 §5 | ⏳ 等 #1 完成后 B 全量复验 | 酒单 B-1 版默认已批准执行 |

## 二、已备好的"后端"预制件（A 直接取用，零拼装）

| 文件 | 内容 | 用法 |
|---|---|---|
| `资料\TEXT注入包.js` | **311 键**一键注入（主 267 + 深潜 dd 32 + 精英 el 12），node --check 通过 | 整体粘进 script（TEXT 定义前不得有其他 TEXT 声明） |
| `manifest.webmanifest`（root） | PWA 清单（C 素材，B 成品化） | 无需动 |
| `sw.js`（root） | Service Worker（缓存优先/版本化/离线兜底），头注释含 3 行注册代码 | 注册即可；发新版改 CACHE 版本号 |
| `资料\TEXT接线映射表.md` | 一期 12 区块 + 二期 A-E 节，逐条"现行片段→TEXT 键→占位符" | 照表替换 |

## 三、上线流程（每版必走）

1. A 完成上表"待做"项 → 自测（node --check + 浏览器全链路跑通）
2. **B 全量验收**（验收清单 §0-§7；TEXT 键数应为 311、雷区 0、语法 OK）
3. A 打包 zip（游戏.html + assets + sw.js + manifest + 说明.txt）
4. 部署 → **diff 校验线上=本地** → 用户手机复验

## 四、v1.0 明确不做（防蔓延）

- 五精英立绘（挂账：等 umodel UE5/wiki/用户截图）
- 匠器 RELICS 实装（B-6 接口已留，v1.1）
- 饰品第二槽、数据节点货币化（待拍板后 v1.1）
- 交易站文案三期接线（映射表二期已备，v1.1 接）

---

*上线判定权：B 验收签字 → A 执行。Rock and Stone！*
