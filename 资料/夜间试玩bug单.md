# 夜间试玩 bug 单（2026-09-16 半夜～08:00 值守）

> 值守人：会话 B。方式：①无头逻辑层压缩试玩（`资料\_soak\_soak.js`，vm 沙箱跑 游戏.html，机器人自动派遣/切双模式/深潜/KPI/离线补算/存读档，07:45 死线）+ ②每小时真浏览器抽检（http://localhost:8765/游戏.html）。
> 本单只记录不改码；修复归属 A（代码）/B（数值文案）。P1=阻断 P2=功能缺陷 P3=体验 数值=经济数值问题。
> 雷区自查：0 命中 ✓

## 一、Bug 主表

| # | 发现时刻(游戏日/现实) | 场景 | 现象 | 复现路径 | 初步定位 | 严重度 | 建议归属 |
|---|---|---|---|---|---|---|---|
| F1 | day~3 / 03:5x | 挂机券·开局 | **开局只有 1 名矿工时，挂机券永远不会自动派遣**——autoPlayStep 的接单门槛是 `idle.length >= 2`，开局单人（或战役只奖到第 2 人之前）券只自动决策事件、不接单，与"自动派遣：所有空闲矿工自动接任务板最高报酬单"的描述不符 | 新档→激活挂机券→侦察兵单人闲置，券到期也不接单 | autoPlayStep 派遣分支 `idle.length >= 2`（游戏.html 约 L2785 附近） | P2 | A |
| F2 | 03:5x | 挂机券·实现脆弱性（非当下 bug） | 托管的事件自动决策走「showEventModal → querySelector('.opt.pri').click()」DOM 点击路径，依赖弹窗按钮类名；无头环境证实该路径脱离 DOM 即哑火。浏览器内当前可用，但类名一改托管就静默失效 | 代码审阅 + 无头复现 | autoPlayStep paused 分支（约 L2788-2793）；建议改为直接调 resolveEvent/autoResolveEvent 的内部映射 | P3 | A |
| F3 | 04:57 / 真浏览器抽检 | **PWA/部署·版本更新** | **回头玩家被 SW 缓存钉死在旧版，永远不会看到新版**。实测：IAB 浏览器打开本地页命中的是 331 键旧构建（还带已退役的加速 ×60/×600 按钮），而磁盘 游戏.html 是 v1.9.1（371 键）；连 `fetch('游戏.html', {cache:'no-store'})` 都被 SW 拦截返回旧内容。清 SW+清缓存后 371 键/v1.9.1 正常 | 任何玩过旧版的浏览器（含用户手机 PWA、drg.manager.mintcat.work 回头访客）直接打开页面即复现，无需特殊操作 | sw.js（B 09-14 备料件）`const CACHE='drg-rig-v2'` 自创建从未更新（mtime 09-14 01:57），而 v1.4→v1.9.1 六个版本全顶着同一缓存名跑「缓存优先」；fetch 处 `caches.match` 命中即返回、运行时 `caches.put` 只增不减 | **P1** | A（一行修复：本版 CACHE 改 `drg-rig-v3`；并立发版流程「每次打包必改缓存版本号」。中期建议： 游戏.html 的 fetch 策略改 network-first、失败回落缓存，从根上免疫忘改版本号） |
| F4 | day 503 / 05:58 | 深潜·编年史口径 | **深潜 ISO 周号用现实时钟，不跟编年史游戏日**——游戏内 2021-09-27（应属 2021-W39），深潜面板显示「本周 2026-W38」，`S.dive.week='2026-W38'`。后果：长线游玩下现实周不翻页→「跨周未通关作废+修正器周抽」永不触发，深潜周刷新机制在编年史口径下失灵（无头 soak 跑到 day 11000+ 深潜一直是同一周，佐证）。这正是节日系统做过、深潜漏掉的同款改造（holidayForNow 已从 `new Date()` 改 `dateOf(gameDay())`） | 游戏内日期≠现实日期的任何存档，打开深潜终端看周号即现 | ensureDiveWeek/isoWeek 取周用 `new Date()` 而非游戏日历日期 | **P2** | A（改法：ISO 周从游戏日历日期算，同 holidayForNow；旧档 S.dive.week 值与新口径不同会自然触发一次跨周刷新，无害零迁移）。口径裁定（B）：**必须跟游戏日**，深潜周=游戏内自然周 |
| F5 | day 503 / 06:02 | 成就文案 | 成就名「☆五十deserve」中英混拼，疑似实装期占位名未定稿 | 系统面板→成就·精通档 | ACHIEVEMENTS JSON 该条 name 字段 | P3 | **B**（文案归 B：对应顾问到访事件"v他50"梗，定名建议「v他50」或「五十桌交情」，早间定稿后交 A 换字符串） |

**【复验记录】**
- F1/F2/F3：A 05:18 推修，**B 06:10 磁盘复验通过**——sw.js CACHE=`drg-rig-v3`（05:09）✓；autoPlayStep 门槛 `idle.length >= 1`（带规格注释）✓；事件决策改内部 `autoResolveEvent(d)` 直调（不再依赖 DOM 类名）✓。F3 的 network-first 为中期建议，未做不算欠账。
- F4/F5：待处理（F4 归 A、F5 归 B 早间定名）。

## 二、数值体感观察（供 B 早间复核 / 非 bug）

- 无头满配压力下 nitra 长期 8 万~10 万堆积：机器人是"满编连派+深潜常开"的极端玩法，暂不下结论；早间结合分钟级真实流速再评估（双模式核算 §一 的闭环流不能直接外推到无头压缩口径）。
- 战役清完→编年史跳跃被高频触发（rush 高 mission 吞吐下尤甚）：day 1300+ 在几分钟内到达属"跳跃机制正常工作"，但**跳跃叠加 rush 刷单**可能是玩家速通编年史的路径，早间评估是否需要"急行模式计数 ×0.5"阀（双模式核算 §七 预留项）。

## 三、误报排除（harness 假阳性，非游戏 bug）

| 现象 | 结论 |
|---|---|
| dep.minerIds 为空数组 | **合法中间态**：hurt() 会把重伤矿工移出派遣（游戏.html L2436），全员重伤时 mid-mission 即空数组，结算走团灭/救援分支 |
| 事件不自动决策导致任务卡住（首轮 smoke 出现） | harness 侧问题：autoPlayStep 依赖 DOM 点击被 stub 吞掉，改用游戏内部 autoResolveEvent 后正常——反过来证实了 F2 的脆弱性 |
| 日志出现一条字面量 `undefined`（D499 13:57，真浏览器） | **观察项暂不立 bug**：仅在「手动 `S.gm += 3天` 跳变」的测试操作后出现一次；自然游玩（无头 1.8 万单 + 浏览器抽检）零复现；全量核对页面 162 处 `TEXT.x` 引用 0 缺键。疑似某分支在非自然时间跳变下的变量为空，A 有空可排查 tick 的日切日志路径 |

## 四、覆盖与未覆盖

- 已覆盖：白手开局→满配全系统、双模式反复切换（含在途切换）、深潜双轨、事件池（含梗/lcyf166/黑脸小猫）自动决策、KPI 多季、钻井平台升级、武器凭证、市场买卖、离线补算 2~48h、存读档往返、编年史跳跃、（预计）day 2316 终点→无尽模式。
- 覆盖外（无头做不到，靠浏览器抽检）：像素动画/精灵图渲染、手机端断点、BGM 实播、抽酒动画、模组抽卡翻牌交互、弹窗视觉层。

## 五、晨报（07:55 收尾时填写）

（待填）
