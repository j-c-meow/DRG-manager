# 部署说明 · 深岩银河 17号钻台管理终端

> 给负责部署的技术同学。这是一个**纯静态单页游戏**（HTML+JS+PNG），不需要任何后端、数据库或构建步骤——任何一个能吐静态文件的 HTTP 服务都能跑。

## 包内容

```
游戏.html          ← 游戏本体（全部逻辑内联，单文件）
assets/
  icons/           ← 官方 Wiki 提取的 55 张 UI 图标
  anim/            ← 像素动画精灵图 32 张（含 9 张梗事件头图；_tools/、_refs/、_preview/ 是构建期产物，本包已剔除）
  pwa/             ← PWA 图标与启动画面
manifest.json      ← PWA 清单（"添加到主屏幕"用）
说明.txt           ← 玩家侧说明
```

**最小可部署集**：`游戏.html` + `assets/`。本包已是净部署集，解压即用。

## 三种部署方式

### 方式 A：Netlify Drop（最快，5 分钟）
1. 解压本包
2. 打开 https://app.netlify.com/drop
3. 把包含 `游戏.html` 的整个文件夹拖进网页 → 秒得 `https://xxxx.netlify.app` 链接
4. 发链接即玩（HTTPS 自动配好，PWA"添加到主屏幕"可用）

### 方式 B：GitHub Pages
1. 建一个 Public 仓库，上传全部文件
2. Settings → Pages → Branch 选 main → Save
3. 链接：`https://<用户名>.github.io/<仓库名>/游戏.html`

### 方式 C：自托管（nginx / 任意静态服务器）
```nginx
server {
    listen 80;
    server_name your.domain;
    root /var/www/drg-idle;
    charset utf-8;              # 重要：文件名含中文，必须 utf-8
    location / { try_files $uri $uri/ =404; }
}
```
- `游戏.html` 可重命名为 `index.html`（同时把 `manifest.json` 里的 `start_url` 改成 `"/index.html"`），根路径直达
- 必须配 `charset utf-8` 或响应头 `Content-Type: text/html; charset=utf-8`，否则中文文件名/内容乱码
- **HTTPS**：PWA 的"添加到主屏幕"在 iOS 上要求 HTTPS；纯 HTTP 游戏本身能玩，只是 PWA 受限

## 存档机制（部署时无需处理）

- 存档在各玩家**自己的浏览器 localStorage**，服务器零存储、零隐私负担
- 游戏内自带：自动存档 + 手动导出/导入 JSON + （Chrome/Edge）绑定本地文件夹同步 `drg_save.json`
- 换域名 = 换 localStorage，存档不跟链接走（玩家可用导出/导入迁移）

## 版本更新

覆盖 `游戏.html` 与 `assets/` 即完成更新。玩家端注意：浏览器可能缓存旧版，让玩家 Ctrl+F5 强刷一次。游戏内有"重置存档"按钮（系统页）。

## 测试码（仅内部测试用，勿公开）

游戏页面直接键盘输入 `jcmeowiscat` 或 `iriscat`：全模组解锁、资源注满、四职业满编、精英全员到齐、钻井平台 Lv10。用于快速验收各系统。

手机端（无键盘）：**系统页 → 测试协议**，输入测试码后点"执行"。

## 版权

粉丝非商用二创，与 Ghost Ship Games 无关；美术素材版权归 GSG。请勿商用、请勿用于任何营利场景。
